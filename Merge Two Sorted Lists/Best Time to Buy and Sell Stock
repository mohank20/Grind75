class Solution {
public:
    int maxProfit(vector<int>& A) {
        int maxval=0,maxdifference=0;
        for(int i=A.size()-1;i>=0;i--){
            maxval=max(maxval,A[i]);
            maxdifference=max(maxdifference,maxval-A[i]);
        }  
        return maxdifference;      
    }
};
